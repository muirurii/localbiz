specVersion: "v1"
serviceId: "my-project"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "my-project-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
